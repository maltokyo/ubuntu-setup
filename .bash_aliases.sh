# malmark below here added

# Insyc Headless get all info
#alias isu='echo "Account Information:" && insync-headless get_account_information && echo "Actions Required:" && insync-headless get_actions_required && echo "Errors:" && insync-headless get_errors && echo "Status:" && insync-headless get_status && echo "Sync Progress:" && insync-headless get_sync_progress && echo "Retrying Errors..." && insync-headless retry_errors'

# Insync Headless Force Sync on Samsung:
#alias isfs='insync-headless force_sync /media/bWes10000/zInsync_mal@g.stippy.com/zInSync/700T1C_bWes10000'

# Docker Aliases
alias dcud='docker-compose up -d'
alias dcu='docker-compose up'
alias dcd='docker-compose down'
alias deit='docker exec -it'
alias dps='docker ps'
# malmark above here added